This directory stores test evidence (screenshots, videos and Allure results).

Do not commit large binary artifacts. Use this folder for generated evidence and add entries to .gitignore if appropriate.

Structure generated by placeholder:
- screenshots/
- videos/
- allure-results/

Replace these placeholders with real evidence produced during test runs. Keep large files out of the repository; consider storing them in external storage or CI artifacts.

